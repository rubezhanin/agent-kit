# Note — short reference / observation

> Origin Return template — see `agent-center/operations/origin-return-protocol/PROTOCOL.en.md`.

```yaml
origin:
owner:
artifact: ./<this-file>.md
status: DONE | BLOCKED | NEEDS_APPROVAL | STALE
return_path:
verification:
```

## TL;DR

Write 1–2 lines: the fact or observation the agent is leaving behind for future-me / future-owner.

## Body

The body of the note. Keep it tight:

- What was observed (input).
- Why it matters (interpretation).
- What to do with it (next step).

## Source ledger

- <source path> — <claim>

## Verification

- How was this checked? (read twice, cross-referenced, sanity-checked, etc.)

## Returned to

- Chat / file / channel where this note was sent back.
